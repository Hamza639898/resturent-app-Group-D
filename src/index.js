import React from 'react';
import ReactDOM from 'react-dom/client';
import "./index.css"; // تأكد من استيراد CSS بشكل صحيح
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);
